[[config]]
template = "/etc/frpc.toml.tpl"
dest = "/etc/frpc.toml"
notifycmd = "sleep 3 && /usr/local/bin/frpc reload -c /etc/frpc.toml" # wait 3 sec to ensure docker will be fully loaded (so that healthcheck can pass)
watch = true

